---
name: VaultForge
description: >-
  将中文课程课件整理为可持续增量维护的 Obsidian 课程知识库：按课次保留教学顺序，提取覆盖全部知识点，生成课程知识网络和可复用的详细知识卡片。
  适用于 PDF、PPT/PPTX、Markdown、TXT、Word 等课程材料。

# VaultForge 课程版

## 主 Agent 与子 Agent 协作协议

VaultForge 由一个主 Agent 负责编排，必要时创建多个职责单一的子 Agent。子 Agent 不自行改变流程、不向用户发起独立确认，也不创建流程之外的文件。

### 主 Agent 的职责

主 Agent 是唯一的流程控制者和用户接口，负责：

1. 读取本文件并决定本次运行的课程目录、语言和模式；
2. 执行阶段 0，生成课程清单并决定 `NEW/UPDATED/DONE`；
3. 在阶段 1 完整解析课件，确认原始材料可用后再分发任务；
4. 建立稳定的 `course_id`、`lesson_id`、`lesson_order` 和 `concept_id`；
5. 创建子 Agent，明确每个子 Agent 的输入文件、输出路径、禁止修改的路径和验收条件；
6. 汇总子 Agent 返回的结构化结果，解决冲突后才更新课程索引和进度文件；
7. 执行最终验证、重试和对用户的唯一一次阶段性汇报。

主 Agent 不应把未经验证的模型自报统计直接写入进度文件，统计必须来自实际文件扫描或子 Agent 的可核验结果。

### 子 Agent 的职责边界

| 子 Agent | 允许写入 | 不允许写入 | 主要输出 |
|---|---|---|---|
| `lesson-organizer` | 指定的新课次笔记 | 课程索引、旧课次、知识卡片 | 课次文件 + 覆盖清单 + 概念候选 |
| `concept-card-builder` | 指定的新卡片；用户未修改卡片的受控增量 | 用户修改/锁定卡片、课次笔记正文 | 卡片文件 + 概念匹配报告 |
| `course-index-manager` | 课程索引、进度文件 | 课次正文、卡片正文 | 索引变更 + 统计 |
| `course-reviewer` | 主 Agent 明确指定的修复文件 | 未授权的历史文件 | 检查结果 + 修复建议 |

### 子 Agent 调度规则

1. 课次之间没有写入冲突时，可以并行，每个子 Agent 最多处理 10 个课次；
2. 概念卡片任务必须在概念候选汇总后执行，不能在候选尚未去重时并行创建同名卡片；
3. 课程索引管理必须在课次和卡片文件稳定后执行；
4. 审查必须在所有写入完成后执行；
5. 任一子 Agent 超时或返回格式错误，主 Agent 只重试该任务，不能盲目重跑整门课程；
6. 不支持子 Agent 时，主 Agent 按同一输入输出协议顺序执行，不能省略覆盖清单和来源范围。

### 子 Agent 任务信封

主 Agent 分发任务时必须包含以下字段：

```yaml
task_id: lesson-L02
agent: lesson-organizer
course_dir: /absolute/path/to/course
course_id: deep-learning
lesson_id: L02
lesson_order: 2
source_files:
  - path: lectures/CNN.pdf
    sha256: sha256:...
allowed_writes:
  - 01. 课程笔记/L02 - CNN.md
context_packet: /tmp/context-L02.json
must_return:
  - output_files
  - covered_points
  - concept_candidates
  - source_ranges
  - warnings
```

子 Agent 返回必须说明：写入了什么、没有写入什么、覆盖了哪些知识点、哪些内容不确定，以及是否需要主 Agent 处理冲突。没有这些字段的返回视为失败。

### 冲突与锁定规则

- 两个子 Agent 认为同一概念需要新建卡片时，主 Agent 先合并候选，再只允许一个卡片任务落盘；
- `user_modified`、`locked` 和不在 `allowed_writes` 中的文件一律只读；
- 课程索引的写入采用最后阶段单写者原则，避免并发追加造成重复链接；
- 子 Agent 不得删除文件、移动文件或修改 Git 历史。

VaultForge 课程版不是通用读书摘要器，而是一个“课程知识整理 Agent”。它把每个课件当作一节课，输出三类稳定对象：

1. `00. 课程索引.md`：整门课的目录、进度和全课程知识网络；
2. `01. 课程笔记/Lxx - 主题.md`：按课件顺序完整讲解本节课，并列出本节知识网络；
3. `02. 知识卡片/概念.md`：对 CNN、RNN、Attention 等可复用概念作独立、详细、可交叉引用的解释。

## 核心原则

### 1. 课次与概念分离

课程笔记回答“老师这一节课如何讲”，知识卡片回答“这个概念本身是什么”。课程笔记必须保留课件顺序；知识卡片按概念组织，不受某一节课的叙述顺序限制。

### 2. 覆盖优先，文件克制

必须覆盖课件中的所有主要知识点，但不是每个知识点都创建一个文件。只有具有独立解释价值、会在后续课程复用、或被用户指定为重点的概念才创建知识卡片；其余内容写入对应课程笔记。

### 3. 证据可回溯

每个课程笔记和知识卡片都记录来源文件、课次和页码/幻灯片范围。PDF 使用 `source_range` 精确抽取；PPTX、DOCX、HTML 由宿主 Agent 读取全文并记录 slide/page 范围。

### 4. 增量优先

新增课件只生成新增课次、覆盖新增内容，并合并已有概念卡片。默认不重写历史课程笔记，不生成 v2/v3 路线图，不生成每次更新报告。

### 5. 用户内容不可被默认覆盖

`vf_status: pristine` 的系统内容可提示刷新；`user_modified` 和 `locked` 的内容禁止自动覆盖。

## 输出目录

```text
课程名/
├── 00. 课程索引.md
├── 01. 课程笔记/
│   ├── L01 - 课程主题.md
│   └── L02 - 课程主题.md
├── 02. 知识卡片/
│   ├── CNN.md
│   └── RNN.md
└── .course-progress.md
```

禁止默认生成以下文件：`Learning Roadmap vN`、`Learning Roadmap (Full)`、每个主题的 MOC、`Core Questions.md`、`Update Report`、争议分析文件。

## 中文交互约定

所有对话、进度、错误和生成内容使用中文。原文引用保留原始语言。确认采用批量方式：`继续`、`修改：具体意见`、`跳过`、`刷新 1,2`。

## 完整工作流

### 阶段 0：课程扫描与增量判定

1. 识别课程目录和已有 `00. 课程索引.md`。
2. 读取 `.course-progress.md` 中的 `processed_lessons`（文件名、hash、lesson_id）。
3. 扫描已有课程笔记和知识卡片，识别 `vf_status`：`pristine`、`user_modified`、`locked`。
4. 将课件标记为 `NEW`、`UPDATED` 或 `DONE`。内容 hash 变化的课件标记为 `UPDATED`。
5. 如果是新课程，建立课程目录；如果是已有课程，默认只处理 `NEW/UPDATED` 课件。

### 阶段 1：课件完整解析

对每个选中的课件完整读取，不能只依据摘要或文件名。提取：

- 课次标题、课次编号、课件顺序；
- 每页/每张幻灯片的标题、正文、公式、图表说明；
- 所有主要知识点及其首次出现位置；
- 知识点之间的前置、组成、对比、扩展、应用关系；
- 原文中的定义、例子、推导和结论；
- 每个知识点的 `source_range`。

必须先完成全部课件读取，再生成课程笔记。大型 PDF 自动分批读取并自动继续，不逐批询问。

### 阶段 2：生成课次笔记

每个课件对应一篇 `Lxx - 主题.md`，内容顺序必须贴近课件，不得把材料完全重排成抽象主题列表。结构：

```markdown
# L02 - 卷积神经网络

## 本节学习目标
## 按课件顺序讲解
### 1. ...
### 2. ...
## 知识点覆盖清单
- [x] 卷积操作
- [x] 参数共享
- [x] 池化
## 本节知识网络
## 关键公式与例子
## 本节知识卡片
## 来源与页码
```

“知识点覆盖清单”必须覆盖课件中的主要知识点；细节知识可以写在对应讲解小节中，不强制单独建文件。

### 阶段 3：抽取、去重和生成知识卡片

从所有新增课件抽取候选概念，按以下顺序决定是否建立卡片：

1. 已有 `concept_id`、标准名或别名命中：复用原卡片；
2. 在本课件多次出现、会在后续课程复用、或用户标记为重点：新建卡片；
3. 仅为局部例子、一次性细节或不具独立解释价值：只写入课次笔记。

概念卡片结构：

```markdown
# CNN

## 一句话定义
## 要解决的问题
## 核心机制
## 工作过程
## 关键公式或直觉
## 常见变体与边界
## 与其他概念的关系
## 在课程中的出现位置
## 来源与原文引用
## 自测问题
```

卡片必须是详细解释，不得只是课件摘要。新增课程再次出现已有概念时，只更新来源课程、补充新变体或应用，不创建 `CNN-1.md` 之类重复文件。

### 阶段 4：建立课程知识网络

使用有限且适合课程的关系类型：

| 关系 | 含义 |
|---|---|
| `prerequisite` | 前置知识 |
| `component` | 组成部分 |
| `contrast` | 对比概念 |
| `extension` | 后续扩展或演化 |
| `application` | 应用关系 |
| `sequence` | 课程讲解顺序 |

每节课在课次笔记中维护局部网络，课程索引维护全课程网络。链接优先写入新增课次和新增/受影响卡片；不要每次更新都重算整个历史知识图谱。所有链接写入前必须去重。

### 阶段 5：轻量验证与进度记录

检查：

- 每个选中的课件都有对应课程笔记；
- 课程笔记按 `lesson_order` 排序；
- 课件主要知识点全部出现在覆盖清单或正文中；
- 每个知识卡片都能反向链接到至少一个课次；
- 概念别名没有生成重复卡片；
- 来源文件和页码/幻灯片范围存在；
- 没有残留 `.tmp` 文件和 `draft/filling` 状态；
- 用户修改或锁定的文件未被覆盖。

更新 `00. 课程索引.md` 和 `.course-progress.md`。不生成额外报告文件；变更历史交给 Git。

## 文件状态

课程笔记和知识卡片使用以下 frontmatter：

```yaml
---
type: lesson | concept
course_id: deep-learning
lesson_id: L02
lesson_order: 2
concept_id: cnn
canonical_name: CNN
aliases: [卷积神经网络, ConvNet]
source_file: CNN.pdf
source_hash: sha256:...
source_range: CNN.pdf:1-42
status: draft | filling | filled | reviewed | needs_review
vf: true
vf_version: course-cn-v1
vf_status: pristine | user_modified | locked
---
```

`status` 表示当前生成阶段，`vf_status` 表示未来是否允许自动更新。文件写入必须遵循 `.tmp → 校验 → rename → 更新状态`。

## 失败恢复与降级

- `filling` 且存在完整 `.tmp`：恢复并标记 `filled`；不完整则清理并重试。
- PDF 无法抽取：报告具体文件，使用原始文件全文回退，不得使用路线图代替原文。
- Agent 不支持并行：每批最多 10 篇，顺序执行。
- 单个概念卡片失败：只重试该卡片，不重跑整门课程。
- 旧文件被用户修改：保留原文，只在课程索引或新增课次中添加链接。

## 脚本

- `scripts/course-manifest.py`：扫描课件 hash、课程笔记和概念卡片，供增量判定使用，不修改课程目录。
- `scripts/context-extractor.py`：按 `source_range` 抽取 PDF/Markdown/TXT 上下文。
- `scripts/double-link-builder.py`：保留作为兼容工具；课程版优先使用有限关系和增量候选，不对整库做无差别重算。

## 完成标准

一次处理完成后，用户只需要看到：新增/更新了哪些课次、建立或更新了哪些知识卡片、覆盖了多少主要知识点，以及是否有需要人工复核的项目。不要生成争议研究、冗余路线图或独立更新报告。
